[project]
name = "hello-world"
version = "0.1.0"
description = "The M0 acceptance fixture: the smallest project that builds."

[build]
target = "wasm32-cli"

[target]
host = "clean-cli"
version = "0.1.0"
